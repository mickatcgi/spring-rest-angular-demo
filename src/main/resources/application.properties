# ===================================================================
# COMMON SPRING BOOT PROPERTIES
#
# ===================================================================

# EMBEDDED SERVER CONFIGURATION (ServerProperties)

# bind to a specific NIC
server.address=localhost 
server.port=8090

# the context path, defaults to '/'
server.context-path=

# LOGGING
logging.path=./log/
logging.file=jetty.log
logging.config=